---
# CUSTOMIZE: article title — shown as the large heading on the article page
title: "Example Article Title"

# CUSTOMIZE: display date shown in the byline, e.g. "May 2025" or "January 2026"
date: "May 2026"

# CUSTOMIZE: your name as it appears in the byline beneath the title
author: "Charlie Ramus"

# CUSTOMIZE: path to the header image, e.g. /images/article-hero.jpg
# Leave empty ("") or remove this line to skip the header image entirely
headerImage: "/images/story1.jpg"

# CUSTOMIZE: full URL to a Google Doc or the original publish location
# Leave empty ("") or remove this line to hide the external link button
externalLink: ""

# CUSTOMIZE: label text for the external link button, e.g. "Read on Google Docs"
# Only used when externalLink is set
externalLinkLabel: "Read on Google Docs"
---

This is the body of your article. Everything below the frontmatter block is rendered
as rich text. You can use standard Markdown here.

## A Heading

Write your paragraphs normally. Line breaks create new paragraphs.

You can use **bold**, _italic_, and `inline code`.

## Another Section

> This is a blockquote. It gets an accent-colored left border and muted italic text.

- List item one
- List item two
- List item three

Links look like [this](https://example.com) — they use the accent color (orange in dark
mode, black in light mode) with an underline on hover.

```js
// Code blocks get a surface background and monospace font
const hello = "world";
```

Replace this content with your actual writing. The slug for this article is
`example-article`, matching the filename `example-article.mdx`.
